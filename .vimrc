Keith Crase
I went through steps 1-3 to start the terminal, locate my home directory /acct/kcrase, and discover my hidden files such as .config. I created this file and 'inserted' the text below before transferring the file over to my laptop using WinSCP. The most difficult process in the lab was using the VPN and WinSCP.

syntax on
set ruler
set autoindent
set number
set ignorecase
color elflord
